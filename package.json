{
  "name": "checkout-web",
  "version": "1.0.0",
  "author": "Andrew Bail <blastoplex@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test --watchAll=false",
    "client:build": "yarn workspace client run build",
    "client:start": "yarn workspace client run start",
    "client:test": "yarn workspace client run test",
    "ds:start": "yarn workspace design-system run start",
    "ds:build": "yarn workspace design-system run build",
    "ds:test": "yarn workspace design-system run test"
  },
  "workspaces": [
    "packages/*"
  ]
}
